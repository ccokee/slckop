apiVersion: v1
kind: Service
metadata:
  name: {{ include "slckop-chart.fullname" . }}
  labels:
    {{- include "slckop-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slckop-chart.selectorLabels" . | nindent 4 }}
